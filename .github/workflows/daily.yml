name: Daily UPSC Current Affairs Run

on:
  schedule:
    # 06:00 IST daily (00:30 UTC)
    - cron: "30 0 * * *"
  workflow_dispatch: {}  # allows manual trigger from the Actions tab

jobs:
  run-agent:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # needed to commit the updated seen-articles DB back

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          # sentence-transformers pulls in torch, which defaults to the full
          # CUDA build (~GBs of nvidia-*/triton/cuda-toolkit wheels) that a
          # CPU-only GitHub-hosted runner never uses. Installing the CPU-only
          # wheel first satisfies the dependency so pip doesn't fetch CUDA.
          pip install torch --index-url https://download.pytorch.org/whl/cpu
          pip install -r requirements.txt

      - name: Run daily pipeline
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_API_KEYS: ${{ secrets.GEMINI_API_KEYS }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
        run: python main.py

      - name: Commit updated seen-articles DB
        run: |
          git config user.name "upsc-agent-bot"
          git config user.email "actions@github.com"
          git add data/articles.db
          git diff --staged --quiet || git commit -m "Update seen-articles DB [automated]"
          git push
